spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bus_booking?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # dev thì để update, sau này prod thì dùng validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

app:
  jwt:
    secret: BAMBOOGARA              # bạn đã chọn secret này
    access-expiration-ms: 900000    # 15 phút
    refresh-expiration-ms: 604800000 # 7 ngày
